"""
The Python Discord API.

The API is used internally as a layer of abstraction
between the Discord bot and the database and is only
available from within the Kubernetes cluster.

The main packages used are FastAPI, pydantic, SQLAlchemy,
and Alembic.
"""
