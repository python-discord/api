"""
Tests for the Python Discord API.

The test modules are structured in a way that mirrors
the `api` package and its subpackages. This should make
it easier to match tests to the functionality they test.

The tests are run using `pytest` as the test runner.
"""
